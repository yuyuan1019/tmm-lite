# TMM-Lite 设计说明书

> 一个运行在 Linux 上的轻量级影视媒体刮削小程序：定时扫描本地电影/剧集目录，
> 从 TMDB + 豆瓣拉取元数据和海报，写入 Kodi/Jellyfin/Emby 兼容的 NFO 文件，
> 并提供一个 Web 管理界面查看和操作。本质是 tinyMediaManager (TMM) 的极简自建版。

---

## 1. 项目目标

- **自动化**：无需人工点击，按计划任务定期扫描媒体库并刮削缺失的元数据。
- **可视化**：提供一个 Web 页面，可以看到刮削状态、手动触发、查看日志、管理媒体库路径。
- **通用输出**：生成的 NFO/海报/背景图遵循 Kodi 元数据标准，Jellyfin、Emby、Kodi 都能直接读取，无需二次转换。
- **轻量部署**：单个 Docker 容器即可运行，数据存本地文件（SQLite + 配置文件），不依赖外部数据库。
- **中文优先**：以 TMDB 为主数据源（结构化、稳定、有官方 API），豆瓣为辅助数据源（补充中文简介/评分，非官方接口，需谨慎调用频率）。

非目标：
- 不做字幕下载、不做转码/压制、不做自动下载（BT/PT 抓种）。
- 不做多用户权限系统，默认单用户内网使用。

---

## 2. 功能需求清单

| 模块 | 功能 |
|---|---|
| 媒体库管理 | 增/删媒体库路径，指定类型（电影 / 电视剧） |
| 扫描 | 遍历媒体库目录，识别每个电影/剧集文件夹，解析并存储条目标题、年份；解析器同时支持季/集识别，但 v1 不遍历或持久化分集信息 |
| 刮削 | 调用 TMDB 搜索 + 详情接口获取元数据；调用豆瓣补充中文简介与评分 |
| 写入 | 生成 `movie.nfo` / `tvshow.nfo`，下载 `poster.jpg` / `fanart.jpg` 到对应目录 |
| 定时任务 | 支持标准 5 段 Cron 表达式，每天/每周自动跑全量扫描+刮削；默认仅处理 pending/failed，开启覆盖 NFO 时 matched 也会重刮 |
| 手动操作 | Web 页面上"立即执行一次"按钮；单条目"重新刮削"按钮 |
| 状态查看 | 列表页展示每个条目的识别标题、匹配标题、评分、状态（待处理/已匹配/失败/需人工/文件缺失）、失败原因 |
| 任务日志 | 记录每次运行的开始/结束时间、成功数、失败数，供排查问题 |
| 设置 | TMDB API Key（支持清除后恢复环境变量回退）、是否启用豆瓣、豆瓣请求间隔、Cron、是否覆盖已有 NFO |

---

## 3. 技术选型建议

| 层 | 选型 | 理由 |
|---|---|---|
| 后端语言/框架 | Python 3.12 + FastAPI | 生态好、异步 HTTP 请求方便、`httpx`/`lxml` 做刮削很顺手 |
| 数据库 | SQLite（通过 SQLAlchemy） | 单机小程序，无需独立数据库服务，文件即数据，方便备份 |
| 定时任务 | APScheduler（AsyncIOScheduler + CronTrigger） | 进程内定时，不需要额外的系统 cron 配置 |
| HTTP 客户端 | httpx（异步） | 配合 FastAPI 异步模型 |
| 页面渲染 | Jinja2 服务端模板 + 原生 HTML/CSS | 不引入前端构建链，单容器即可运行，维护成本低 |
| 部署 | Docker + docker-compose | 挂载媒体库目录、配置目录为 volume，一次构建到处运行 |

> 如果开发者更熟悉 Node.js 或 Go，同样的分层设计可以 1:1 迁移，核心逻辑（解析器 / 刮削器 / NFO 生成器 / 调度器 / Web 路由）与语言无关。

---

## 4. 系统架构

```
                ┌─────────────────────────────┐
                │      Web 浏览器 (管理员)      │
                └───────────────┬─────────────┘
                                │ HTTP
                ┌───────────────▼─────────────┐
                │   Web/API 层 (FastAPI 路由)  │
                │  - 媒体库管理                │
                │  - 刮削结果查看/手动触发       │
                │  - 设置                      │
                └───────┬───────────────┬──────┘
                        │               │
             ┌──────────▼──────┐   ┌────▼─────────────┐
             │  调度器 (Scheduler)│   │  数据层 (SQLite)  │
             │  Cron 定时触发全量  │   │  Library         │
             │  扫描+刮削任务      │   │  MediaItem       │
             └──────────┬──────┘   │  ScrapeLog        │
                        │           └───────────────────┘
             ┌──────────▼───────────────┐
             │     扫描与刮削主流程        │
             │ 1. 遍历磁盘目录 → 生成/更新 │
             │    MediaItem 记录          │
             │ 2. 文件名解析器 提取        │
             │    标题/年份/季集           │
             │ 3. 调用刮削器获取元数据      │
             │ 4. 下载图片 → 最后写 NFO    │
             └──┬─────────────────────┬──┘
                │                     │
      ┌─────────▼──────┐     ┌────────▼────────┐
      │  TMDB 刮削器     │     │  豆瓣刮削器(辅助) │
      │  官方 REST API   │     │  非官方接口,      │
      │                  │     │  需限流           │
      └──────────────────┘     └───────────────────┘
                │
      ┌─────────▼──────────────┐
      │  本地媒体库目录 (挂载卷)  │
      │  /media/movies/...      │
      │  /media/tvshows/...     │
      └──────────────────────────┘
```

---

## 5. 目录结构约定

程序自身（代码仓库）建议结构，仅作参考，非强制：

```
tmm-lite/
├── docker-compose.yml
├── Dockerfile
├── requirements.txt / package.json
├── data/                      # 挂载卷：配置文件 + SQLite 数据库
│   ├── config.yaml
│   └── tmm-lite.db
└── app/
    ├── main.py                 # Web/API 入口
    ├── config.py               # 配置读写
    ├── database.py             # 数据模型 + DB session
    ├── scheduler.py            # 定时任务
    ├── scanner.py              # 扫描 + 刮削主流程
    ├── nfo_writer.py           # NFO 生成
    ├── parsers/
    │   └── filename_parser.py  # 文件名/目录名解析规则
    ├── scrapers/
    │   ├── tmdb.py
    │   └── douban.py
    ├── templates/               # Web 页面
    └── static/
```

**媒体库目录约定**（扫描器依赖这个约定去识别条目；发现是**递归**的，分组/深层结构同样支持）：

- 电影：一部电影一个文件夹，文件夹内放视频文件。文件夹名建议格式 `片名 (年份)`，例如：
  `/media/movies/星际穿越 (2014)/Interstellar.2014.1080p.mkv`
- 电影亦支持：
  - 分类/导演/系列分组嵌套，如 `/media/movies/动作/星际穿越 (2014)/Interstellar.2014.1080p.mkv`；
  - 视频位于更深子目录（如 `片名 (2020)/Video/电影.mkv`、`片名/BDMV/STREAM/*.m2ts`）；
  - 库根目录直接放置的散视频文件——每个散文件视为一个条目。
- 电视剧：一部剧一个文件夹，文件夹名 `剧名 (年份)`；其下可选按季分子文件夹 `Season 01`/`S01`/`第1季`；
  集文件名需包含 `S01E02` 或中文 `第01集` 格式，例如：
  `/media/tvshows/繁花 (2023)/Season 01/繁花.S01E01.mkv`

---

## 6. 数据模型

### 6.1 `Library`（媒体库）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | 整数主键 | |
| name | 字符串 | 展示名，如"电影" |
| path | 字符串，唯一 | 容器内挂载路径 |
| media_type | 枚举 movie / tv | |

### 6.2 `MediaItem`（一个电影或一部剧）
| 字段 | 说明 |
|---|---|
| id | 主键 |
| library_id | 所属媒体库 |
| media_type | movie / tv |
| folder_path | 磁盘路径，唯一；文件夹条目为目录路径，库根散文件条目为视频文件完整路径 |
| parsed_title / parsed_year | 从文件名解析出的原始标题/年份（刮削前） |
| status | pending（待处理）/ matched（已匹配）/ failed（失败）/ manual_needed（解析失败需人工处理，见第 8 节）/ missing（磁盘文件夹已删除，见 7.1 第 3 步） |
| source | v1 固定为 tmdb；豆瓣仅补充简介/评分，不改变主来源 |
| source_id | 对应数据源的条目 ID |
| imdb_id | IMDb ID（`tt…`），刮削后保存，用于字幕精确匹配与 NFO uniqueid |
| matched_title | 刮削后确认的标题 |
| matched_original_title / matched_year | 刮削结果中的原始标题/年份 |
| overview | 简介 |
| rating | 评分 |
| poster_url / backdrop_url | 图片地址 |
| genres | 类型标签，逗号分隔 |
| last_scraped_at | 最近一次刮削时间 |
| error_message | 失败原因，便于排查 |

### 6.3 `ScrapeLog`（每次任务运行记录）
| 字段 | 说明 |
|---|---|
| id | 主键 |
| started_at / finished_at | 开始/结束时间 |
| total / matched / failed | 本轮进入自动处理队列的数量及其最终结果；全跳过时均为 0 |
| detail | 可选的详细文本日志 |

### 6.4 `AppMeta`（内部元数据）
键值表，用于保存 `libraries_seed_imported` 等一次性初始化标记。媒体库是否首次导入不能只根据
Library 表是否为空判断，否则用户删除全部库后重启会再次导入 YAML seed。

---

## 7. 核心流程

### 7.1 全量扫描 + 刮削（定时任务与"立即执行"共用同一入口）

1. 读取配置里的所有 `Library`。
2. 对每个库的路径做目录遍历：
   - 电影库：每个含视频文件的子文件夹视为一个条目。
   - 剧集库：每个子文件夹视为一部剧，不下钻季/集；季集解析能力仅作为后续分集 NFO 的预留。
3. 将磁盘发现的条目与数据库 `MediaItem` 做增量同步。只有媒体库根路径被成功完整枚举时，才把其中未发现的数据库条目置为 `missing`；路径不存在、权限错误或 NAS 临时掉线时只记录日志，该库条目也不进入本轮刮削队列。missing/manual 再发现且有可跳过 NFO 时先转 pending，入队后统一计为 matched；否则按解析结果转 pending/manual_needed。
4. NFO 状态矩阵：pending 遇已有 NFO且未开启覆盖时可跳过；failed 标题有效时即使旧 NFO 仍在也必须重试，标题仍为空则回到 manual_needed；matched 的 NFO 被删除后转 pending/manual_needed。开启覆盖后 matched 也进入队列强制重刮。
5. 默认筛选 `pending`/`failed`；覆盖开关开启时再加入 `matched`。`manual_needed`/`missing` 不进入自动队列。
6. 对每个条目：
   a. 调用 TMDB 搜索（电影用 `/search/movie`，剧集用 `/search/tv`，带 `year` 参数辅助命中）。
   b. 命中后调用详情接口取完整字段。遇 429 按 `Retry-After` 等待，首次请求外最多重试 3 次（总尝试 4 次）。联网前先完成本地 NFO 跳过项；401/未配置 Key 时，只把当前及剩余 API 必需项置 failed 后停止请求。
   c. 若启用豆瓣：用解析出的标题搜索，并用 TMDB 匹配年份二次校验；豆瓣模块和编排层均隔离异常，失败不影响 TMDB 主流程。
   d. 若 TMDB 返回图片 URL，先原子下载海报为 `poster.jpg`、背景图为 `fanart.jpg`。
   e. 图片完成后，最后原子写入 `movie.nfo` 或 `tvshow.nfo`，将 NFO 作为完整产物标记。
   f. 更新状态为 `matched`；NFO 写入或存在 URL 的图片下载失败均设为 `failed` 并记录原因，下轮可重试。
7. 汇总本次统计，写一条 `ScrapeLog`。

**并发保护**：全量、定时和单条重刮共用进程内任务占位标志。Web 后台启动必须在返回响应前同步占位，重复触发直接拒绝。部署固定为单容器、单 Uvicorn worker；多进程或多副本需要外部锁和独立调度器，不属于 v1。

### 7.2 手动重新刮削单条目
Web 页面对某一行点击"重新刮削"，只对该条目重复步骤 7.1 第 6 步（无视已有 NFO，强制覆盖）。失败在方法内转成条目 failed 并写一条任务日志，不返回 500。

---

## 8. 文件名解析规则（关键容错点）

需要处理的典型噪音：分辨率（1080p/2160p/4K）、编码（x264/x265/H.264）、来源（BluRay/WEB-DL/HDTV）、发布组标签、字幕组中文标签（国语/中字/高清）等，解析时应统一清洗掉，只保留：

- **年份**：优先取括号内的四位年份（如 `片名 (2009)`）；无括号时取文件夹名中**最后一个**匹配 `(19|20)\d{2}` 的年份（避免 `2012 (2009)`、`1917 (2019)` 这类片名本身是年份的电影解析错误）。
- **标题**：去除年份及以上噪音词、下划线/点号替换为空格、合并多余空格后即为标题。中文媒体库常见的开头序号（`14.奇异博士1`）一并剥离，仅当分隔符后紧跟中文字符时生效，避免误伤 `50.First.Dates`、`007：…` 这类真实片名。
- **季号**：优先匹配 `S01E02` 形式的 S 段；退化匹配 `Season 01` 或裸 `S01`。
- **集号**：优先匹配 `S01E02` 的 E 段；退化匹配中文 `第01集` / `第1话`。

季/集必须从完整名称提取，不能先按年份截断，否则会漏掉 `剧名.2023.S01E01.mkv`。v1 仅交付
解析函数及测试，不扫描分集、不把 season/episode 写入数据库。

解析失败且没有可直接复用 NFO 的条目应标记为 `manual_needed`，不进入自动队列；已有 NFO 且
未开启覆盖时，可先进入 pending 并按本地 NFO 完成，不需要标题或外部请求。

---

## 9. 刮削数据源设计

### 9.1 TMDB（主数据源，官方 API）
- 需要在 themoviedb.org 免费申请 API Key。
- 关键接口：
  - `GET /search/movie?query=&year=&language=zh-CN`
  - `GET /search/tv?query=&first_air_date_year=&language=zh-CN`
  - `GET /movie/{id}?language=zh-CN`
  - `GET /tv/{id}?language=zh-CN`
- 图片基础地址：`https://image.tmdb.org/t/p/original{poster_path}`。
- **限流处理**：首次刮削会连续调用 search+detail，必须处理 429：按 `Retry-After` 等待，首次请求外最多重试 3 次（总尝试 4 次），耗尽后记为条目 failed。不要依赖一个固定的服务端每秒请求数。
- 每次 TMDB search/detail API 请求需带 `language=zh-CN`；图片 CDN 请求不追加。中文字段为空时的英文 fallback 留作扩展。

### 9.2 豆瓣（辅助数据源，非官方）
- **没有官方公开 API**，只能通过页面/建议接口做非官方抓取，需明确告知使用者以下风险：
  - 请求频率必须限制（建议 ≥2 秒/次），否则容易被限流或封 IP。
  - 页面结构随时可能变化导致解析失效，需要有独立的异常捕获，豆瓣失败不能影响 TMDB 主流程。
  - 仅建议用于个人媒体库场景，不用于高频或商业用途。
- 用途仅限于：补充中文简介、补充评分。不作为主匹配源（不用豆瓣的搜索结果决定"是不是这部电影"，命中判断以 TMDB 为准），且覆盖前必须做年份二次校验（见 7.1 第 6.c 步），防止同名不同年的条目错误覆盖。

---

## 10. NFO 文件规范

采用 Kodi 元数据标准，Jellyfin/Emby 均兼容识别，无需转换：

**movie.nfo**
```xml
<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<movie>
  <title>片名</title>
  <originaltitle>原始片名</originaltitle>
  <year>年份</year>
  <rating>评分</rating>
  <plot>简介</plot>
  <genre>类型1</genre>
  <genre>类型2</genre>
  <uniqueid type="tmdb" default="true">TMDB条目ID</uniqueid>
</movie>
```

**tvshow.nfo** 结构类似，根节点为 `<tvshow>`。

图片命名遵循 Kodi 约定：`poster.jpg`（海报）、`fanart.jpg`（背景图），放在条目根目录即可被识别，无需额外配置。

> 扩展方向：如需分集刮削，可在每集视频同目录生成同名 `.nfo`（如 `S01E01.nfo`），根节点为 `<episodedetails>`，字段类似但增加 `<season>` `<episode>` 标签。本期设计未覆盖分集级刮削，仅做到剧集整体信息。

---

## 11. Web 管理界面（页面清单）

| 路径 | 页面 | 内容 |
|---|---|---|
| `/` | 仪表盘 | 媒体库数量、已匹配/待处理统计、"立即执行一次"按钮、当前任务运行状态（运行中/空闲）、最近一次任务概况 |
| `/libraries` | 媒体库管理 | 增加/删除媒体库路径与类型，列表展示各库条目数 |
| `/items` | 刮削结果 | 表格展示所有条目：识别标题、年份、类型、匹配标题、评分、状态徽标、失败原因、单条"重新刮削" |
| `/logs` | 任务日志 | 历次运行的起止时间与成功/失败统计 |
| `/settings` | 设置 | TMDB API Key（可清除）、是否启用豆瓣、豆瓣请求间隔、定时任务 Cron、是否覆盖已有 NFO |

## 12. 后端路由/接口清单（供开发对照实现）

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/` | 仪表盘页面 |
| POST | `/run-scrape` | 手动触发一次全量扫描+刮削（任务运行中时返回拒绝提示） |
| POST | `/stop-scrape` | 请求停止当前运行的扫描（空闲时返回错误提示） |
| POST | `/rescrape-failed` | 一键重新刮削所有失败条目（后台，限速） |
| GET | `/libraries` | 媒体库列表页 |
| POST | `/libraries/add` | 新增媒体库（表单：name, path, media_type） |
| POST | `/libraries/{id}/delete` | 删除媒体库（不删磁盘文件） |
| GET | `/items` | 刮削结果列表页 |
| POST | `/items/{id}/rescrape` | 单条目重新刮削 |
| POST | `/items/{id}/subtitle` | 单条目手动下载字幕 |
| POST | `/items/{id}/delete` | 删除当前记录（仅删记录，不删磁盘文件；路径记入忽略列表） |
| POST | `/items/clear-ignored` | 清空忽略列表 |
| GET | `/logs` | 任务日志页 |
| GET | `/settings` | 设置页 |
| POST | `/settings` | 保存设置（TMDB key/清除 key / 豆瓣开关 / 间隔 / cron / 覆盖已有 NFO 开关） |
| GET | `/healthz` | Docker 健康检查，返回 `{"status":"ok"}` |

> 如果未来需要给其他程序（如 NAS 面板）调用，建议在以上表单式路由之外，额外暴露一组 `/api/*` 的 JSON 接口（返回结构与上述数据模型字段一一对应），当前设计的路由已可直接改造为 JSON 接口，无需推翻架构。

---

## 13. 配置文件设计（`data/config.yaml`）

```yaml
tmdb_api_key: ""              # YAML 可空；为空时回退 TMDB_API_KEY 环境变量
use_douban: true              # 是否启用豆瓣补充信息
douban_delay_seconds: 2.0     # 豆瓣请求间隔，必须为有限数字且 >= 0.5
overwrite_existing_nfo: false # 全量任务是否覆盖已有 NFO（默认跳过，保护手工数据）
language: "zh-CN"             # TMDB 返回语言
schedule_cron: "0 4 * * *"    # 标准 5 段 cron：分 时 日 月 周，默认每天凌晨4点
libraries: []                 # 可选首次导入 seed；默认空，建议启动后在 Web 中添加
```

如需首次启动自动导入，可把 `libraries` 改为包含 `name/path/type` 的列表；path 必须是容器内
绝对路径。成功处理一次后写入数据库内部标记，此段后续不再读取。

**单一事实来源约定**（避免双写冲突）：

- **媒体库列表**：数据库是唯一事实来源。`config.yaml` 中的 `libraries` 只在内部首次导入标记不存在时处理一次；处理后即写标记，不能仅以 Library 表为空判断。之后修改 YAML 不再生效。
- **设置项**（API Key、豆瓣开关、间隔、cron 等）：以 `config.yaml` 为唯一持久化载体，Web「设置」页保存时直接改写该文件，程序重启后仍生效。
- **TMDB API Key 优先级**（从高到低）：Web 设置页保存的值（即写入 `config.yaml` 的值）> 环境变量 `TMDB_API_KEY`。YAML 为空时回退环境变量；设置页提供“清除已保存 Key”选项。

---

## 14. 定时任务设计

- 使用标准 5 段 Cron 表达式（分 时 日 月 周），默认值 `0 4 * * *` 表示每天凌晨 4 点执行一次全量流程。
- 配置保存和调度器使用同一个 `CronTrigger.from_crontab` 校验入口，必须先校验后落盘，避免保存调度器无法解析的表达式。
- 用户在「设置」页修改 Cron 后，调度器应支持**热更新**；任务运行中暂不允许修改设置，避免替换正在使用的 HTTP client。
- 定时任务与"立即执行"按钮触发的是**同一段核心逻辑**（见第 7.1 节），避免维护两套流程。
- 已 `matched` 的条目默认跳过；开启覆盖已有 NFO 后，matched 也会进入每次全量任务。

---

## 15. 部署方案

- **打包**：单容器 Docker 镜像，基础镜像建议 `python:3.12-slim`（或对应语言的官方精简镜像）。
- **数据持久化**：`data/` 目录挂载为 volume，保存配置文件与数据库文件，容器重建不丢数据。
- **媒体库挂载**：媒体库目录必须以读写方式挂载，因为程序需要生成 NFO/图片；路径由用户在 `docker-compose.yml` 中映射。
- **端口**：默认对外暴露 `8000`，供 Web 管理界面访问。
- **环境变量**：`TMDB_API_KEY`（仅在 Web 设置页/config.yaml 未填写时作为回退，优先级见第 13 节）。
- **时区**：容器内时区需与宿主机一致（如 `Asia/Shanghai`），否则定时任务触发时间会有偏差。
- **进程模型**：v1 使用 root、单容器副本、单 Uvicorn worker。root 用于简化 NAS 写权限配置，风险必须写入 README；非 root 支持列入 v1.1。

---

## 16. 非功能性需求

- **限流保护**：豆瓣抓取必须有请求间隔，且失败要有独立的异常捕获，不能影响 TMDB 主流程或让整个任务崩溃；TMDB 遇 429 需按 `Retry-After` 退避重试（见 9.1）。
- **并发保护**：全量任务与单条重刮共用进程内同步占位标志，任务运行中重复触发直接拒绝；该机制依赖单进程部署（见 7.1）。
- **幂等性**：重复扫描同一目录不应产生重复的 `MediaItem` 记录（以 `folder_path` 唯一约束保证）。
- **可观测性**：每次任务运行必须落一条 `ScrapeLog`，单条目失败要记录具体 `error_message`，方便在 Web 页面上直接定位问题（不用去翻容器日志）。
- **容错**：普通单条失败不能中断整批；TMDB 认证失败时仅将剩余 API 必需项记为 failed，本地 NFO 可跳过项仍为 matched。豆瓣异常在模块和编排层双重隔离。
- **事务**：外部 HTTP 等待期间不得持有 SQLite 写事务；扫描同步、逐条状态更新和日志更新使用短事务。
- **敏感信息**：日志、异常和 `error_message` 不得包含 TMDB 请求 query 中的 API Key。
- **可扩展性**：架构上刮削器（TMDB/豆瓣）以独立模块存在，未来新增数据源（如 Bangumi 弹幕番组计划、IMDb）只需新增一个刮削器模块并接入第 7.1 步的第 6 步逻辑，不影响其余部分。
- **安全性**：Web 界面无认证，仅限内网使用，**切勿直接暴露到公网**；如需外网访问应置于反向代理之后并自行加认证（Basic Auth / VPN）。可选扩展点：内置简单的 Basic Auth 开关。

---

## 17. 后续可扩展方向（本期不实现，供路线图参考）

- 分集级别刮削（每集独立 NFO + 剧照）。
- 手动匹配 UI：当自动搜索结果不准确时，弹出候选列表让用户选择正确条目。
- 多数据源候选对比与优先级配置（如中文标题优先豆瓣、评分优先 TMDB）。
- Webhook/通知（任务完成后推送到微信/Telegram/邮件）。
- 与 Jellyfin/Emby API 联动，刮削完成后触发媒体库自动扫描刷新，无需等待其自身定时扫描。

---

*本文档为架构与需求说明，用于指导后续开发（人工或 AI 均可据此实现代码），不含具体实现代码。*
