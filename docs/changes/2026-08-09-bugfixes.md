# 2026-08-09 缺陷修复

## 修复内容

- 修复扫描后台任务竞态、字幕保存目录与扩展名、扫描状态和异常收尾。
- 加固本地、SFTP、WebDAV 连接及 HTTP 单字节区间流式读取。
- 消除播放器、预览页和 TMDB 候选列表中的元数据注入路径。
- 完善配置严格校验、热更新回滚与资源生命周期管理。
- 增加旧数据库迁移、扫描期间媒体库变更保护及远程连接参数校验。

## 测试结果

- Python 3.12.10：369 passed，1 skipped。
- 总覆盖率 79.89%；文件名解析器 95.9%；扫描器 91.3%。
- Ruff、mypy、compileall、覆盖率阈值和 `git diff --check` 均通过。

## 提交信息

- `d1bc93d` chore: normalize repository text files
- `d751512` fix: harden runtime security and compatibility

## 字幕下载链路追加修复

- 网络代理现在同时作用于 ASSRT、OpenSubtitles 和 SubDL，不再仅供 TMDB 使用。
- OpenSubtitles 改用正确的 `/download` JSON 协议，按 IMDb ID 优先搜索，并内置浏览器 User-Agent。
- SubDL 改用官方 API 域名和 API Key，正确解析 `subtitles`/`unpack_files`，支持安全提取 ZIP 字幕。
- 默认首选语言改为简体中文 `zh-cn`，旧配置 `chi,zho,zh` 仍会自动映射。
- 鉴权、网络、限流和下载错误不再伪装成“未找到字幕”，任务日志会显示具体失败源。

本轮全量回归：380 passed，1 skipped；Ruff、mypy 与 `git diff --check` 通过。
